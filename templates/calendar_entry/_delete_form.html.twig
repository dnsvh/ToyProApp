<form method="post" action="{{ path('app_calendar_entry_delete', {'id': calendar_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendar_entry.id) }}">
    <button class="btn">Delete</button>
</form>
