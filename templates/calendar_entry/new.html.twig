{# templates/calendar_entry/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create new Calendar entry{% endblock %}

{% block body %}
    <h1>Create new Calendar entry</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_calendar_entry_index') }}">back to list</a>
{% endblock %}
